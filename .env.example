# This dApp is a fully client-side static frontend. It needs NO secrets: it reads
# and writes the live contract through the user's browser wallet. The deployer key
# is read only at deploy time from the repo-root .env and is never bundled or committed.